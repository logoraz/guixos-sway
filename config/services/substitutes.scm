;;; WIP - Create a service to handle package substitutes (& transformations)
;;;  Goal is to be applicable to both System & Home packages
(define-module (config services substitutes)
  #:use-module  (guix transformations)

  )
